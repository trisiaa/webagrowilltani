name: webagro
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  fl_chart: ^0.69.0
  chopper: ^8.0.3 # Check for the latest version
  json_serializable: ^6.1.5 # For JSON serialization
  shared_preferences: ^2.0.0
  intl: ^0.18.0
  http_parser: ^4.0.0
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.3.0
  chopper_generator: ^8.0.3

flutter:
  uses-material-design: true
  assets:
    - lib/assets/
